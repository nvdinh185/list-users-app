
class Greeting {

    // Hàm tạo (constructor)
    constructor() { }

    // Phương thức của class
    hello() {
        console.log(123456789);
    }
}

// Xuất các biến và hàm
module.exports = new Greeting();
